# dependencies
/node_modules
**/node_modules/
/.pnp
.pnp.js

# testing
/coverage
/test-results

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
# `.env*` e não só `.env`/`.env*.local`: um `.env.production` ou um
# `.env.staging` não estavam cobertos e entrariam num `git add -A` sem
# um aviso. A exceção é o exemplo, que é para estar versionado.
.env*
!.env.example

# vercel
.vercel

# relatórios gerados pelo CI / monitores
fiscal-check-report.md
audit-report.txt

# typescript
*.tsbuildinfo
next-env.d.ts

.vercel

# Claude Code — worktrees temporários (criados internamente, não pertencem ao repo)
.claude/worktrees/

# artefacto binário do pdf.js copiado localmente (1,3 MB, regenerável, não referenciado)
/public/pdf.worker.min.mjs

# Documentos compostos (gerados por `npm run docs`)
/out

# Cache do Python das funções da Vercel
__pycache__/
*.pyc
